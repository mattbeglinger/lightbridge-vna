# =========================
# Terraform (Infrastructure)
# =========================
# Local .terraform directories (plugins & backend config)
.terraform/

# Terraform State files (Contains sensitive IDs - NEVER commit these)
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform Vars (Contains your actual AWS/Azure secrets)
*.tfvars
terraform.tfvars
!terraform.tfvars.example

# Crash logs
crash.log
override.tf
override.tf.json

# NOTE: .terraform.lock.hcl is intentionally NOT ignored.
# It should be committed to ensure consistent provider versions.

# =========================
# Helm / Kubernetes (Software)
# =========================
# Ignore the specific config files with your passwords
values-primary.yaml
values-secondary.yaml

# Ignore downloaded dependency charts (the .tgz files)
charts/**/charts/
*.tgz

# NOTE: Chart.lock is intentionally NOT ignored.
# It pins exact dependency versions for reproducible builds.

# =========================
# OS & IDE System Files
# =========================
# Mac
.DS_Store

# Windows
Thumbs.db

# VS Code / JetBrains
.vscode/
.idea/
*.swp
